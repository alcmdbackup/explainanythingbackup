# Read-only connection to staging Supabase PostgreSQL.
# Uses dedicated readonly_local role with SELECT-only privileges.
# Format: postgresql://readonly_local.<project-ref>:<password>@aws-1-us-east-2.pooler.supabase.com:5432/postgres
STAGING_READONLY_DATABASE_URL=
