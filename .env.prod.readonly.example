# Read-only connection to production Supabase PostgreSQL.
# Uses dedicated readonly_local role with SELECT-only privileges.
# Format: postgresql://readonly_local:<password>@db.<project-ref>.supabase.co:5432/postgres
PROD_READONLY_DATABASE_URL=
