// Re-export shim for backward compatibility with scripts that import from this path.
// Production code moved to deferred/lib/arenaUtils.ts in M8.
export { addEntryToArena } from '../deferred/lib/arenaUtils';
