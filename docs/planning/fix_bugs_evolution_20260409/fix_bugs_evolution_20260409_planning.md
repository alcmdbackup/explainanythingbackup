# Fix Bugs Evolution 20260409 Plan

## Background

This project fixes several bugs and UX gaps in the Evolution pipeline admin UI. Key issues include cost display discrepancies where run list entries show cost exceeding the budget cap while detail pages show lower values, incorrect "hide test content" filtering that excludes non-test runs, and broken dropdown/filter controls in the logs tab. Additional work includes adding jump-to-page navigation across all entity list views, fixing an incorrect "explanation" column in the run list view, adding agent name filtering to the invocation list view, and covering all fixes with E2E/integration/unit tests.

## Requirements (from GH Issue #938)

- Cost for some runs can exceed components
    - Runs c4057835 and eb62d393 have cost exceeding budget on run list overview entry
    - Cost on details is much lower
    - Verify this via playwright
- Run c4057835 only has 4 variants
- Hide test content on runs list view hides runs not related to test
    - c4057835 is NOT a test run, figure out why its being hidden
- Improve logs tab for runs (and also generally)
    - Add a way to jump to a given page (including last page)
    - Dropdowns and filters all seem broken. Fix and then add E2E/integration/unit tests
- List view improvements
    - All entity list views should have ways to jump to a given page
    - Improve list of filters based on UX
    - Invocation list view specifically should allow filtering based on agent name
- Run list view has a column called "explanation" - this seems wrong

## Problem

The Evolution admin UI has accumulated several data display bugs and UX gaps. The "hide test content" filter incorrectly hides all runs due to a Supabase NOT IN syntax bug (string literal instead of array). The run detail metrics tab shows duplicate cost cards because two cost-tracking systems both write to `evolution_metrics` with labels that collide. The runs list "explanation" column never shows explanation data because `explanation_id` is missing from the SELECT query. LogsTab filters have UX issues including exact-match agent filter and missing debounce. Jump-to-page pagination is absent from all list views.

## Options Considered

- [x] **Option A: Fix each bug independently**: Address each bug in isolation with targeted fixes and dedicated tests per issue.
- [ ] **Option B: Fix bugs + shared pagination/filter refactor** *(not chosen — Option A selected)*: Fix bugs while extracting shared pagination and filter components to avoid duplication across all entity list views.
- [ ] **Option C: Fix bugs only, defer UX improvements** *(not chosen — Option A selected)*: Fix the three data/filtering bugs first, then tackle UX improvements (pagination jump, filters) separately.

## Phased Execution Plan

### Phase 1: Investigate & Reproduce Bugs ✅ COMPLETE
- [x] Investigate cost discrepancy — **root cause found**: two cost-tracking systems write to `evolution_metrics`; run list sums invocations ($0.31), detail "Total Cost" card shows budget-tracker value ($0.05). Underlying cost tracking being fixed elsewhere; this project fixes the UI display.
- [x] Investigate why run c4057835 only has 4 variants — **not a bug**: 9 `generate_from_seed_article` invocations ran, 5 variants were rejected. Budget was $0.05 and was exceeded. No fix needed.
- [x] Investigate "hide test content" false positive — **root cause found**: `.not('strategy_id', 'in', string)` should be `.not('strategy_id', 'in', array)`. Same bug in 4 files.
- [x] Investigate broken logs tab — **root cause found**: agent filter uses `.eq()` not `.ilike()`, iteration dropdown type mismatch (number vs string), missing debounce on text inputs.
- [x] Investigate explanation column — **root cause found**: `explanation_id` not included in SELECT query. User decision: add it to SELECT so the column shows explanations when available.
- [x] Audit all entity list view filters for gaps

### Phase 2: Fix Core Data & Filter Bugs
- [x] **Fix hide test content NOT IN syntax** — change `query.not('col', 'in', \`(${ids.join(',')})\`)` to `query.not('col', 'in', ids)` in all 5 locations:
  - `evolution/src/services/evolutionActions.ts:224` (runs filter)
  - `evolution/src/services/evolutionActions.ts:600` (variants filter)
  - `evolution/src/services/invocationActions.ts:82` (invocations filter)
  - `evolution/src/services/evolutionVisualizationActions.ts:83,91` (visualization filter)
  - `src/__tests__/integration/evolution-test-content-filter.integration.test.ts:108,167` (test uses same broken syntax)
- [x] **Fix explanation column** — the `evolution_runs.explanation_id` column was dropped in V2 migration (noted in `20260322000006_evolution_fresh_schema.sql:70`). Write a new migration: `ALTER TABLE evolution_runs ADD COLUMN IF NOT EXISTS explanation_id BIGINT REFERENCES explanations(id) ON DELETE SET NULL;` (`explanations.id` is `bigint generated by default as identity`, confirmed in `20251109053825_fix_drift.sql`). Then add `explanation_id` to SELECT in `getEvolutionRunsAction` (`evolutionActions.ts:212`); the batch-fetch enrichment code (lines 256-277) is already correct and will work once the field is selected
- [x] **Fix MetricGrid duplicate key bug** — `MetricItem` interface has no `name` field; fix by: (1) adding `id?: string` to `MetricItem` interface (`MetricGrid.tsx:6`), (2) passing `id: row.metric_name` through `toMetricItem()` in `EntityMetricsTab.tsx:56`, (3) changing `key={metric.label}` to `key={metric.id ?? metric.label}` in MetricGrid render
- [x] **Hide duplicate agentCost:* metrics** in `EntityMetricsTab` — filter out `agentCost:*` metric rows from the raw `metrics` array before the `metrics.map()` call at line 111 (i.e., add `const filteredMetrics = metrics.filter(m => !m.metric_name.startsWith('agentCost:'))` before line 111 and use `filteredMetrics.map(...)` instead), since `total_generation_cost`/`total_ranking_cost` supersede them
- [x] Write unit tests for hide test content filter (array format, empty array edge case)
- [x] Write unit tests for MetricGrid key uniqueness with duplicate labels

### Phase 3: Fix Logs Tab Filters & Dropdowns
User confirmed all filters are broken. Fix each one and trace the full filter → server action → Supabase query path end-to-end:
- [x] **Level dropdown** — confirm `.eq('level', filters.level)` applied in `logActions.ts`; verify "All" option sends `undefined` not empty string; verify onChange wired in `LogsTab.tsx`
- [x] **Entity type dropdown** — confirm `.eq('entity_type', filters.entityType)` applied; verify "All" option sends `undefined`; verify hidden correctly for invocation entity pages
- [x] **Iteration dropdown** — fix `LogsTab.tsx:136`: change `value={i}` to `value={String(i)}` so selected value visually highlights; confirm `.eq('iteration', filters.iteration)` applied correctly as number in `logActions.ts`
- [x] **Agent/phase text filter** — fix `logActions.ts:69`: change `.eq('agent_name', filters.agentName)` to `.ilike('agent_name', \`%${filters.agentName}%\`)`; add 300ms debounced state in `LogsTab.tsx` (same pattern as `debouncedMessage` at line 50-53); apply same SQL wildcard escaping pattern used for messageSearch (`replace(/[%_\\]/g, '\\$&')`)
- [x] **Message search** — confirm debounce fires correctly and `.ilike('message', \`%...%\`)` is applied; verify SQL wildcard escaping
- [x] **Variant ID filter** — confirm `.eq('variant_id', filters.variantId)` applied in `logActions.ts`; add 300ms debounce in `LogsTab.tsx` matching messageSearch pattern

### Phase 4: Pagination Jump-to-Page
- [x] **Add jump-to-page input + "Last Page" button to EntityListPage** — number input between page buttons and Next; Enter or button press calls `onPageChange(clampedPage)`; Last button calls `onPageChange(totalPages)`
- [x] **Add jump-to-page to LogsTab** — number input in pagination bar; Enter calls `setOffset((pageNum-1) * PAGE_SIZE)`. LogsTab has `currentPage`, `totalPages`, `offset`, `PAGE_SIZE` available.
- [x] Applies automatically to all 6 entity list pages since they all use EntityListPage
- [x] Write unit tests for pagination component (jump-to-page, last page, boundary conditions: page < 1, page > totalPages)

### Phase 5: Invocation List Filters & UX Improvements
- [x] **Add agent name filter to invocations** (`invocations/page.tsx` + `invocationActions.ts`) — add `agentName: z.string().optional()` to Zod schema, add `.ilike('agent_name', \`%${parsed.agentName}%\`)` to query, add `{ key: 'agentName', label: 'Agent Name', type: 'text' }` to FilterDef
- [x] **Add name search to experiments list** — add text filter to `experiments/page.tsx` and `.ilike('name', \`%${filters.name}%\`)` to `experimentActions.ts:listExperimentsAction` (note: this action uses raw typed input, not Zod — add Zod schema consistent with other actions before adding the filter param)
- [x] **Add name search to prompts list** — add text filter to `prompts/page.tsx` and `.ilike('name', \`%${filters.name}%\`)` to `arenaActions.ts:listPromptsAction`
- [x] **Add strategy filter to runs list** — backend already supports `strategy_id` param; fetch strategies list at page load using existing `listStrategiesAction` from `strategyRegistryActions.ts` (no V2 suffix), populate a select FilterDef with `{ value: s.id, label: s.name }` options
- [x] Write unit/integration tests for new filter combinations

### Phase 6: E2E Coverage
- [x] Write E2E spec for run list: hide test content toggle works correctly (non-test run c4057835 visible), explanation column shows run ID / explanation link
- [x] Write E2E spec for logs tab: agent name filter matches partial names, iteration dropdown shows selected value, pagination jump works
- [x] Write E2E spec for invocation list: agent name filter works
- [x] Write E2E spec for experiments/prompts: name search filters correctly
- [x] Tag critical tests with `{ tag: '@critical' }`

## Testing

### Unit Tests
- [x] `evolution/src/services/evolutionActions.test.ts` — test "hide test content" filter uses array format NOT IN
- [x] `evolution/src/components/evolution/primitives/MetricGrid.test.tsx` — render MetricGrid with two items sharing label "Generation Cost" but different `id` values; assert no React key warning and both items render
- [x] `evolution/src/components/evolution/tabs/EntityMetricsTab.test.tsx` — render with metrics including both `agentCost:generation` and `total_generation_cost`; assert only one "Generation Cost" card appears (agentCost:* filtered out)
- [x] `evolution/src/components/evolution/tabs/LogsTab.test.tsx`:
  - Each filter (level, entityType, iteration, agentName, message, variantId) triggers a reload when changed
  - Agent filter uses ilike (partial match), not eq
  - Iteration dropdown option values are strings so selected item visually highlights
  - Agent and variantId inputs are debounced; message input is debounced
  - Pagination resets to page 1 when any filter changes
  - "All" options send `undefined` to the server action, not empty string
- [x] `evolution/src/components/evolution/EntityListPage.test.tsx`:
  - Jump-to-page: valid page calls onPageChange with correct page number
  - Jump-to-page: value below 1 clamps to 1; value above totalPages clamps to totalPages
  - "Last" button calls onPageChange(totalPages)
  - Page input clears after navigation

### Integration Tests
- [x] `src/__tests__/integration/evolution-test-content-filter.integration.test.ts` — fix broken NOT IN syntax in test (lines 108, 167), verify filter correctly excludes only test-strategy runs
- [x] `src/__tests__/integration/evolution-logs-filters.integration.test.ts` — one test per LogsTab filter against real DB:
  - level filter returns only logs of that level
  - entityType filter scopes to correct entity type
  - iteration filter returns only that iteration's logs
  - agentName partial match returns logs where agent_name contains the substring
  - messageSearch returns only logs where message ilike matches
  - variantId filter returns only logs for that variant
  - Combined filters (level + agentName) return intersection

### E2E Tests
- [x] `src/__tests__/e2e/specs/09-admin/admin-evolution-runs.spec.ts` (extend existing):
  - Hide test content: non-test runs visible when unchecked, hidden when checked
  - Explanation column: shows run ID fallback when no explanation; shows title link when explanation_id present
  - Cost display: run list cost visible and does not show duplicate cards on detail
  - Strategy filter: filters run list to selected strategy
  - Jump-to-page: entering page number navigates; Last button goes to last page
- [x] `src/__tests__/e2e/specs/09-admin/admin-evolution-logs.spec.ts` (extend existing):
  - Level dropdown: selecting "error" shows only error-level logs
  - Entity type dropdown: selecting "invocation" shows only invocation logs
  - Iteration dropdown: selecting iteration 1 shows only iteration 1 logs; selected value visually highlighted
  - Agent/phase filter: partial text match ("generation") returns relevant logs
  - Message search: text appears in log messages
  - Variant ID filter: filters to specific variant
  - Jump-to-page: navigates to correct page
  - Combined filters: multiple filters applied simultaneously narrow results correctly
- [x] `src/__tests__/e2e/specs/09-admin/admin-evolution-invocations.spec.ts` (extend existing):
  - Agent name filter: partial match works
  - Jump-to-page on invocations list
- [x] `src/__tests__/e2e/specs/09-admin/admin-evolution-experiments-list.spec.ts` (extend existing): name search filters correctly
- [x] `src/__tests__/e2e/specs/09-admin/admin-strategy-registry.spec.ts` (extend existing): name search on prompts list
- [x] Tag all new tests with `{ tag: @critical }`

### Manual Verification
- [x] Toggle "hide test content" on runs list — confirm non-test runs remain visible
- [x] Visit run c4057835 detail Metrics tab — confirm no duplicate "Generation Cost" cards
- [x] Runs list explanation column — confirm shows explanation link when available, run ID when not
- [x] Exercise all logs tab filters including agent name partial match
- [x] Navigate to last page via jump-to-page in entity lists
- [x] Filter invocations by agent name partial match
- [x] Search experiments by name, prompts by name

## Verification

### A) Playwright Verification (required for UI changes)
- [x] Run `npx playwright test src/__tests__/e2e/specs/09-admin/admin-evolution-runs.spec.ts`
- [x] Run `npx playwright test src/__tests__/e2e/specs/09-admin/admin-evolution-logs.spec.ts`
- [x] Run `npx playwright test src/__tests__/e2e/specs/09-admin/admin-evolution-invocations.spec.ts`

### B) Automated Tests
- [x] `npm test -- --testPathPattern="evolutionActions|MetricGrid|LogsTab|EntityListPage"`
- [x] `npm run test:integration -- --testPathPattern="evolution-test-content-filter|evolution-infrastructure"`
- [x] `npm run test:e2e:critical`

## Documentation Updates
The following docs were identified as relevant and may need updates:
- [x] `evolution/docs/visualization.md` — update if paginator or filter components change significantly
- [x] `evolution/docs/data_model.md` — update if cost query or metrics usage changes
- [x] `evolution/docs/logging.md` — update if LogsTab filter behavior changes
- [x] `evolution/docs/metrics.md` — update if cost metric aggregation changes
- [x] `docs/docs_overall/testing_overview.md` (no update needed — test methodology unchanged) — update test statistics if new specs added

## Review & Discussion

### /plan-review — 2026-04-09 (4 iterations, consensus at 4/5)

**Iteration 1** (2/5, 2/5, 1/5): Critical gaps — MetricItem interface has no `name` field (key fix plan incorrect), `explanation_id` migration needed, strategy service filename wrong (`V2` suffix doesn't exist), `experimentActions.ts` not `V2`, `agentCost:*` hiding needed a concrete implementation description, E2E spec paths referenced non-existent `admin-evolution.spec.ts`, no unit test for agentCost:* hiding.

**Iteration 2** (2/5, 2/5, 1/5): Remaining gaps — `explanation_id` type still `INTEGER` but `explanations.id` is `BIGINT`; agentCost filter description said "before items.filter()" but no such call exists; MetricGrid test too vague; EntityMetricsTab test for agentCost:* missing.

**Iteration 3** (3/5, 3/5, 3/5): Gaps resolved — BIGINT type corrected, filter location clarified ("filter `metrics` array before line 111"), concrete test scenarios added for MetricGrid and EntityMetricsTab.

**Iteration 4** (4/5, 5/5, 4/5): ✅ **CONSENSUS — ready to execute.** No critical gaps. Minor notes: LogsTab iteration dropdown value type and agent filter .eq()→.ilike() are correctly documented in plan as implementation tasks, not planning gaps.
