# LLM Costs Too Low In Dashboard Plan

## Background
Evolution dashboard / cost reporting says ~$3 spent in the past week, but real total is more like $40-60. Figure out why by querying Supabase dev and making sure tests are adequately accounted for. Backfill if necessary.

## Requirements (from GH Issue #1263)
- The dashboard ("evolution docs say $3 spent in past week") under-reports LLM spend; real total is ~$40-60 for the same window.
- Investigate root cause by querying **Supabase dev** (`npm run query:staging`, read-only) — compare reported vs actual spend.
- Make sure **tests are adequately accounted for** — confirm the `is_test` discriminator isn't wrongly excluding real operational spend (or wrongly including test spend in the "real" figure).
- **Backfill if necessary** — repair historical/missing cost data where a valid join key exists.

## Problem (confirmed via dev DB — see research doc)
The `/admin/costs` dashboard reads ALL its spend numbers from `llmCallTracking` (summary card, by-model, by-user via direct query; stacked/by-entity via the `get_llm_spend_buckets` RPC; daily via the `daily_llm_costs` view). But `llmCallTracking` is **incomplete**: evolution LLM calls almost never write a row (only 10 of 21,841 invocations over 30d on dev; 479/517 recent real invocations still missing — the audit-gap persists). The evolution source of truth lives separately in `evolution_agent_invocations.cost_usd` (+ `evolution_metrics`). Result: `/admin/costs` sees ~$0.006/30d of evolution spend versus ~$38/30d actual. Separately, **89% of evolution cost is genuine test-strategy spend** that is also filtered/invisible. The foundational defect: there is **no single complete cost ledger** — any consumer that reads `llmCallTracking` under-reports total spend by the evolution amount, and this will recur for every new cost surface.

## Goal (per user decision)
Fix `/admin/costs` to stop under-reporting AND do **foundational rework** so a canonical, complete cost source captures **all** spend (evolution + non-evolution) — so current and future cost consumers cannot silently under-report. Scope: Dev only for now.

> **Canonical file note:** there is exactly ONE cost-analytics module — `evolution/src/services/costAnalytics.ts` (it backs `/admin/costs` despite the `evolution/` path; `src/lib/services/costAnalytics.ts` does NOT exist). All read paths (`getCostSummaryAction`, `getDailyCostsAction`, `getCostByModelAction`, `getCostByUserAction`, `getSpendByGranularityAction`, `getCostByEntityAction`, `getEvolutionReconciliationAction`, `backfillCostsAction`) live there, with its colocated test `evolution/src/services/costAnalytics.test.ts`.

## Options Considered
- [ ] **Option A: Complete the ledger at the source (write-path fix)**: Make every evolution LLM call reliably write a `llmCallTracking` row (genuinely fail-closed on whatever dev path drops them; resolve Open-Q5 — runner agents not using the `requireTracking`-configured provider). Pros: truest "one ledger". Cons: historical gap stays (unbackfillable); must find why writes drop; double-write risk needs dedup discipline. **Does not fix history → not load-bearing.**
- [ ] **Option B-DB (REJECTED — design unsound): DB-level UNION view/RPC of `llmCallTracking` + `evolution_agent_invocations`.** Rejected after plan-review: `evolution_agent_invocations` has NO `model`/`total_tokens`/`userid`/`call_source`/`is_test` columns and is **per-phase grain** (its `cost_usd` aggregates many LLM calls) vs `llmCallTracking`'s per-call grain — so a column-compatible, dedup-safe UNION is not soundly constructible, and by-model/by-user breakdowns are meaningless at invocation grain.
- [x] **Option B-APP (CHOSEN, revised 2026-06-23): app-level canonical merge in `evolution/src/services/costAnalytics.ts`.** Evolution spend is sourced at **invocation grain** — `SUM(evolution_agent_invocations.cost_usd)` bucketed by `invocation.created_at` — by EXTENDING the existing `getEvolutionReconciliationAction` (`evolution/src/services/costAnalytics.ts:692`), which already SUMs invocation cost as the reconciliation truth. **Why invocation grain, not `getRunCostsWithFallback` (run-grain `evolution_metrics` rollup):** (a) it matches the reconciliation oracle's anchor exactly → exact parity, no window-edge disagreement; (b) it counts in-flight/unfinalized runs' spend immediately (the rollup is written only `at_finalization`, so a run-grain source would re-introduce a smaller under-report for live runs — the very bug we're fixing); (c) `/admin/evolution-dashboard` answers a different question (per-run finalized cost) so a small documented difference between the surfaces is correct, and the existing audit-gap banner already compares these two. Non-evolution spend comes from `llmCallTracking` filtered via `attributeCallSource(call_source).category !== 'evolution'` (the mandated, future-proof dedup predicate — excludes the ~$0.04 of evolution rows in `llmCallTracking`; no FK join, no double count). Merged inside the costAnalytics actions; evolution appears as a single `evolution` category/entity (honest about no per-model/per-user breakdown at invocation grain). Pros: extends existing correct reconciliation code, single time anchor, counts live spend, correct across the historical gap, minimal SQL (one index). Cons: app-level merge logic per action; requires a `created_at` index on `evolution_agent_invocations`.
- [x] **Option C: B-APP now + Option A write-path hardening (CHOSEN)**: app-level canonical merge fixes the dashboard immediately (incl. history); write-path hardening (Phase 3) makes the underlying ledger trend complete going forward. Sequence: B-APP does NOT depend on A.
- [x] **Option D: Test-spend classification (CHOSEN — cross-cutting)**: evolution test/real split via the `evolution_runs → evolution_strategies.is_test_content` join (3-table reach from invocations; at run grain it's `run.strategy_id → strategy.is_test_content`); non-evo via `llmCallTracking.is_test`. NULL/orphan strategy classifies as **real** (conservative — never hide real spend). Test spend becomes a visible line, not a filtered-out bucket.
- [ ] **Option E (rejected): Backfill `llmCallTracking` rows for the gap window** — documented non-backfillable (no per-call token data / join key). Data already exists in `evolution_metrics`/`evolution_agent_invocations`; reconstructing the audit table is not the fix.

## Phased Execution Plan (Option C + D)

### Phase 0: Root cause of trackingless evolution spend (gates Option A) — Open Q5 [RESOLVED 2026-06-23]
Confirmed by code trace + DB + runner journal (supersedes earlier wrong hypotheses — see Research Q5 history):
- [x] **Production path is CORRECT.** `processRunQueue → claimAndExecuteRun.ts:204-223` routes every call through `callLLM` with `requireTracking:true` + `trackingDb`; `createEvolutionLLMClient.ts:206` calls `rawProvider.complete` (that same provider), so `requireTracking` is preserved end-to-end (the "Agent.run rebuild drops it" guess was WRONG). Runner journal shows `callLLM completed successfully`; the ~49 evolution `llmCallTracking` rows / 30d come from here. The runner is mostly idle ("No pending runs"), and the claim-gate makes it skip `[TEST]` strategies entirely.
- [x] **The bypass is the DIRECT-PROVIDER dev/experiment path.** `evolution/scripts/run-evolution-local.ts`'s `createDirectLLMProvider` (line 190) calls the SDK directly (`.messages.create:210`, `.chat.completions.create:276`) and computes cost via `calculateLLMCost`, **never routing through `callLLM`/`saveTrackingAndNotify`** — why it's in the `check-llm-call-coverage` ALLOWLIST. **Correction (plan-review iter4):** it is NOT "zero rows" — it ALSO does a *direct* `llmCallTracking.insert` (lines 227, 294) on the `if (supabase)` path, but those rows are **unjoinable/unreliable**: `call_source='evolution_v2_${label}'`, `userid='0000…0000'` (all-zero, not `EVOLUTION_SYSTEM_USERID`), **NO `evolution_invocation_id`**, fire-and-forget with swallowed errors. So a join on `evolution_invocation_id` finds ~0 (matching the observed gap), and on the no-DB fallback path (dummy `localhost:54321` client) the inserts silently fail. Net: real LLM output + `cost_usd` in invocations, but no joinable/trustworthy `llmCallTracking`. (Dedup note: `evolution_v2_*` still `startsWith('evolution_')`, so `attributeCallSource` folds it to the `evolution` category and the dashboard's non-evo filter correctly excludes it — no double-count.)
- [ ] **Remaining (small):** confirm which harness produced the specific staging `[TEST]`-strategy batch runs (run-evolution-local.ts vs an integration/experiment script) — all share the direct-provider-bypass shape.
- [ ] **Option A scope (DECIDED 2026-06-23): track the direct-provider spend.** Route `run-evolution-local.ts` through the shared tracked provider so it writes `llmCallTracking`, and remove its coverage-guard allowlist entry — implemented in Phase 3 Layer 2 below. **Does not block Option B** (the dashboard reads `evolution_agent_invocations`, which these paths already populate).

### Phase 1: App-level canonical merge in `evolution/src/services/costAnalytics.ts` (Option B-APP + D)
- [ ] Add an evolution-spend helper that, for a `[start,end]` window, queries `evolution_agent_invocations` (`created_at` in range, `cost_usd`), joined `run_id → evolution_runs.strategy_id → evolution_strategies.is_test_content`, and returns/aggregates `SUM(cost_usd)` per `date_trunc(granularity, created_at)` × `is_test`. **Single time anchor = `invocation.created_at`**, matching `getEvolutionReconciliationAction`'s SUM. Refactor that action to share this helper so the reconciliation oracle and the dashboard read identical math (no drift) — the oracle's scalar-SUM contract is preserved as the bucket-collapsed, `includeTest`-on case of the helper, and both the oracle and the dashboard pass identical `includeTest`/window args so parity holds in every toggle state.
- [ ] **Add unconditional migration `CREATE INDEX IF NOT EXISTS idx_invocations_created_at ON evolution_agent_invocations(created_at)`** (none exists today; the windowed SUM + the scheduled Layer-3 reconciliation both scan by `created_at` on a 30k+ row table). Idempotent; gated by `migration:verify`.
- [ ] **Dedup = filter, not join (mandated predicate):** filter the non-evolution `llmCallTracking` queries with `attributeCallSource(call_source).category !== 'evolution'` (future-proof — catches any future non-`evolution_`-prefixed evolution writer) so evolution rows in `llmCallTracking` are excluded; evolution spend comes solely from the invocation helper. No FK join, no grain mixing.
- [ ] Merge in each affected action: `getCostSummaryAction` (total = nonEvo + evo), `getSpendByGranularityAction`/`getCostByEntityAction` (evolution as a single `evolution` category/entity per bucket), `getCostByModelAction`/`getCostByUserAction` (evolution as one `evolution-pipeline` row — explicitly note per-model/per-user is unavailable at invocation grain).
- [ ] **`getDailyCostsAction` EXCEPTION:** it reads the pre-aggregated `daily_llm_costs` view (no `call_source`/`is_test` columns), so the dedup filter and test/real split are inapplicable. Scope decision: leave `getDailyCostsAction` on its current `llmCallTracking`-only view (documented existing follow-up to add `is_test` to the view); do NOT attempt the evolution merge there in this project. Note this caveat in the UI.
- [ ] First-class `is_test` (Option D): evolution via `strategy.is_test_content`; non-evo via `llmCallTracking.is_test`. Honor the existing `includeTest` toggle on BOTH sides. (`evolution_runs.strategy_id` is NOT NULL FK, so orphan/NULL strategy is structurally impossible — keep a defensive `COALESCE(is_test_content,false)` ⇒ real, but it's not a live path.)
- [ ] **No DB UNION view/RPC.** `get_llm_spend_buckets` keeps its exact `SECURITY DEFINER` signature, unchanged (the granularity merge happens app-side after calling it for the non-evo side). Prefer app-side bucketing; the only migration is the index above.
- [ ] **Staged rollout / rollback:** gate the new merge with env flag `COST_DASHBOARD_UNIFIED_EVOLUTION`; unset reverts to current `llmCallTracking`-only reads with no code rollback.

### Phase 2: `/admin/costs` UI — show complete + split spend (Option D)
- [ ] Headline "Total Cost" reflects the canonical total (evolution + non-evolution), no longer ~$0 for evolution.
- [ ] Test vs real shown as a first-class split (stacked chart / By-Entity), not just toggled out.
- [ ] Keep/retire the audit-gap banner depending on whether the canonical read makes it moot.

### Phase 3: Write-path hardening — the "can't silently under-report" guarantee (Option A, 3 layers)
The foundational ask: evolution writes `llmCallTracking` going forward, and *anything* that calls an LLM without recording spend is caught. Three complementary layers (none alone is sufficient — rationale below). **Note:** the real fail-closed gate is `src/lib/services/llms.ts:301` (`if (options?.requireTracking) throw trackingError;`); line 286 is only a log field. Evolution call sites ALREADY pass `requireTracking:true` (`claimAndExecuteRun.ts:223`, arenaActions, runJudgeEval, runPromptEditorConfig), so evolution is already fail-closed *at the chokepoint* — the live bug is calls not reaching it (Phase 0), not a missing default.

- [ ] **Layer 1 — Keep evolution fail-closed + add a kill-switch (do NOT flip the system-wide default).** Confirm every evolution entrypoint passes `requireTracking:true`. Add an env kill-switch (e.g. `LLM_REQUIRE_TRACKING_DISABLED`) so fail-closed can be reverted without a deploy if it starts failing runs. **Explicitly OUT OF SCOPE:** flipping the main-app default to fail-closed — that fail-closes 16 non-evolution `callLLM` sites (stream-chat, aiSuggestion, tagEvaluation, links, etc.) which intentionally swallow tracking blips (`llms.ts:255-256`); a global flip is a separate, risk-assessed decision, not part of this dev-only project.
- [ ] **Layer 2 — Route the direct-provider script through the tracked chokepoint + tighten the CI guard (DECIDED 2026-06-23: track them).**
  - [ ] Extract `claimAndExecuteRun.ts:197-248`'s inline tracked `llmProvider` into a shared factory in `evolution/src/lib/pipeline/infra/trackedEvolutionProvider.ts` — `createTrackedEvolutionProvider({ db, defaultModel, maxOutputTokens })`. It must reproduce ALL of the inline provider's `callLLM` args: `evolutionSource(label)` for `call_source`, `EVOLUTION_SYSTEM_USERID` for userid, `allowedLLMModelSchema.parse(opts.model ?? defaultModel)`, `EVOLUTION_MAX_OUTPUT_TOKENS`, `trackingDb: db`, `requireTracking: true`, and a **per-call** `evolutionInvocationId: opts.invocationId` + `onUsage` closure (declared inside `complete()`, not hoisted). Returns `{ text, usage }` — accepted by both the runner and the script (their consumers discriminate `typeof res === 'string' ? res : res.text`, so the return-shape change is a non-issue). Refactor the runner to use it (behavior-preserving). Enabler: `callLLM` is already CLI-safe (`processRunQueue → claimAndExecuteRun` imports it from `@/lib/services/llms` and runs under `tsx` — the script's "avoid Next.js" rationale is stale post-06-21).
  - [ ] **`run-evolution-local.ts` — DELETE `createDirectLLMProvider` ENTIRELY** (BOTH call sites use the factory: line 434 + line 443 seed-model — full-function deletion forces the compiler to surface both). The whole function (lines ~190-313) must go, including BOTH SDK calls (`.messages.create:210`, `.chat.completions.create:276`) AND BOTH direct `from('llmCallTracking').insert(` (lines 227, 294) AND the now-orphaned `import OpenAI` (14), `import Anthropic` (15), `import { calculateLLMCost }` (19). **Keep** the `SupabaseClient` type import (still used by `getSupabase`) and the line-343 `evolution_runs.insert` (only `llmCallTracking` inserts are the bypass). **The `check-llm-call-coverage` guard scans raw file text** — leaving any dead bypass code makes `npm run lint` (which runs `check:llm-coverage`) FAIL. Keep `createMockLLMProvider` (no SDK, returns `{text,usage}`) for `--mock`. Verify `npm run check:llm-coverage` passes against the REAL edited file, not just the unit test's synthetic inputs.
  - [ ] Remove `'evolution/scripts/run-evolution-local.ts'` from the `check-llm-call-coverage` ALLOWLIST → any future direct-SDK pipeline provider fails CI. Leave the manual probe/benchmark scripts (`test-qwen3-thinking.ts`, `test-oss20b-thinking.ts`, `test-judge-models-v2.ts`, `benchmark-latency.ts`) allowlisted (one-off, not pipeline spend) with a documented reason; `oneshotGenerator.ts` already self-tracks.
  - [ ] **Handle the `LLMSpendingGate` fail-closed regression (REQUIRED, not optional).** Routing through `callLLM` forces `getSpendingGate().checkBudget()`, which internally builds its OWN `createSupabaseServiceClient()` (it IGNORES the injected `trackingDb`) and **fails closed** — `getKillSwitch` throws on DB error, `reserveViaRpc` throws `GlobalBudgetExceededError` on error. So: (i) **real-LLM runs now require real DB env** (`NEXT_PUBLIC_SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`) — without them the gate's service client can't reach the DB and every call is blocked; document that `--mock` is the DB-less path. (ii) Add a **required** `--ungated` flag (or env) for dev experimentation that must run without caps. **Mechanism (specify — `callLLM:988` calls `checkBudget()` unconditionally, there is NO per-call skip param):** `--ungated` is satisfied by pointing at a dev DB whose `llm_cost_config` caps are set high (so the gate passes) — NOT by threading a skip into `callLLM`; document that `--mock` remains the truly DB-less path. Add a small unit test for the flag's wiring. (iii) With DB present, fail-closed-on-real-cap is intended (the `LLM_REQUIRE_TRACKING_DISABLED` kill-switch covers the tracking-write throw separately).
  - [ ] **Side-effects to document:** (a) global semaphore — `call_source` starts `evolution_` so factory calls acquire `getLLMSemaphore()`, serializing the local script with the production runner against the same dev DB (latency/contention); (b) `--model` now goes through `allowedLLMModelSchema.parse()` → an unregistered model id throws a Zod error instead of a live call (the "pass any model" affordance is lost — accepted trade-off); (c) `is_test` — a real CLI run lands `is_test=false` (operational evolution spend) unless a runtime flag tags it.
- [ ] **Layer 3 — Reconciliation monitor (the durable guarantee; catches the silent-divergence class).** Add `evolution/scripts/checkTrackingReconciliation.ts` that, per recent window, compares evolution spend from the **source of truth** (`evolution_metrics`/`evolution_agent_invocations`, per-run SUM — NOT a naive 1:1 row-existence check, since one invocation = N tracking rows) against the `llmCallTracking` evolution total (reuse the shape of the existing `getEvolutionReconciliationAction`, `evolution/src/services/costAnalytics.ts:692`). Wire as a `schedule:`-cron GitHub workflow modeled on **`evolution-cost-alarm.yml`** (files `[release-health]` issues via `gh issue create` — NOT `evolution-run-health.yml`, which is Slack-only), **staging-only** (honor dev-only scope). Define a threshold (e.g. invocation-total > tracking-total × 1.5, mirroring the dashboard banner). **It is a scheduled job, NOT a required PR status check** — so a legitimately-RED reconciliation files an issue and never blocks CI/merges. **Expected RED until Phase 0/Layer 2 lands** — that is the point: it is the detector that proves the write path is fixed and stays fixed. (The Layer-3 *integration test* is separate and MUST pass deterministically on seeded fixtures — it asserts the reconciliation function correctly flags/clears divergence on controlled data, not that live reconciliation is currently green.)

**Rationale (why 3 layers):** Layer 1 throws only for calls that reach the chokepoint; Layer 2 (static CI) is the only thing that can see a call that bypasses the chokepoint via direct SDK; Layer 3 catches the residual "configured correctly yet no row appears" case that neither a runtime throw nor a static scan detects — exactly the failure mode observed in this investigation.

### Phase 4: Verify + guard against regression
- [ ] Re-run the dev reconciliation query; confirm `/admin/costs` evolution total now equals the `evolution_agent_invocations` invocation-grain SUM **exactly** (same source/anchor as `getEvolutionReconciliationAction` by construction) and splits test/real correctly.
- [ ] Confirm all three Phase-3 enforcement layers are live (Layer 1 default-on throw, Layer 2 coverage guard green, Layer 3 reconciliation assertion wired) — the foundational "can't silently under-report" guarantee.

## Testing

### Unit Tests
- [ ] `evolution/src/services/costAnalytics.test.ts` (EXISTING — extend) — `getCostSummaryAction` total = nonEvo (`llmCallTracking` filtered via `attributeCallSource(...).category !== 'evolution'`) + evo (invocation-grain SUM, mocked); `includeTest` toggles both sides; evolution test/real split via mocked `strategy.is_test_content`; no double-count (evolution rows in `llmCallTracking` excluded by the filter); also assert an `unattributed:`-prefixed row classifies as `non_evolution` and is NOT pulled into the evolution bucket. **Time-bucket boundary case:** an evolution invocation whose `created_at` lands on a `date_trunc` boundary lands in the same UTC bucket the non-evo side uses (no off-by-one / timezone split).
- [ ] `src/lib/services/llmCostAttribution.test.ts` (verify exists; else colocate) — `attributeCallSource` folds evolution sources to the `evolution` category (the mandated dedup predicate).
- [ ] `src/lib/services/llms.test.ts` (module is `src/lib/services/llms.ts`) — **Layer 1**: an evolution call (`requireTracking:true`) throws on tracking-write failure; a representative **main-app call (no `requireTracking`) still swallows** (blast-radius guard — proves the default is NOT flipped); **kill-switch precedence**: with `LLM_REQUIRE_TRACKING_DISABLED` set, even a `requireTracking:true` call swallows (assert the env check wins over the `:301` gate, so rollback is provably exercised).
- [ ] `scripts/check-llm-call-coverage.test.ts` (EXISTING) — **Layer 2 regression anchor**: assert `ALLOWLIST.has('evolution/scripts/run-evolution-local.ts') === false` (the load-bearing negative assertion coupling the test to the real allowlist edit). The "new direct-SDK file is flagged" case already exists via synthetic inputs. **Plus a non-test gate:** the executor must run `npm run check:llm-coverage` against the real edited `run-evolution-local.ts` (raw-text scan) and confirm green — the unit test's synthetic inputs do NOT cover the real file.
- [ ] `evolution/src/lib/pipeline/infra/trackedEvolutionProvider.test.ts` (NEW) — `createTrackedEvolutionProvider` calls `callLLM` with `trackingDb` + `requireTracking:true` + `evolutionSource(label)` + `EVOLUTION_SYSTEM_USERID` + `allowedLLMModelSchema.parse` + `evolutionInvocationId: opts.invocationId`; assert the per-call `onUsage` closure populates the returned `usage` (the `capturedUsage` path being preserved). Behavior-preserving vs the old inline `claimAndExecuteRun` provider — include a runner-side assertion that the extracted factory yields the same `{text,usage}` the inline provider did (guards the bare-string→`{text,usage}` return-shape change).

### Integration Tests
- [ ] `src/__tests__/integration/evolution-cost-attribution.integration.test.ts` (EXISTING — extend) — over a seeded window, canonical merge evolution total == `getEvolutionReconciliationAction` invocation SUM **exactly** (same source/anchor now — not "within tolerance"); plus non-evolution `llmCallTracking` SUM.
- [ ] **Layer 3** reconciliation test (in the above file or a new `tracking-reconciliation.integration.test.ts`) — per-run SUM comparison flags divergence when invocations have cost but no `llmCallTracking` rows, passes when present. Seeded evolution run MUST set `allow_test_execution:true` (or use `executable:true` factory option) so the post-2026-06-21 claim-gate doesn't skip it (testing_overview Pattern A-2). Pin the `describe` title so `--grep` selects it.

### E2E Tests
- [ ] NEW `src/__tests__/e2e/specs/09-admin/admin-costs-evolution-total.spec.ts` (NOT the existing `admin-evolution-cost-split.spec.ts`, which tests unrelated per-purpose columns; reconcile with the existing `admin-costs-dashboard.spec.ts` regression guard) — `/admin/costs` headline reflects evolution truth (non-zero); test vs real split renders. Tagged `@evolution`. Include `afterAll` cleanup per ESLint `flakiness/require-test-cleanup` if it imports DB tools.

### Manual Verification
- [ ] `npm run query:staging` reconciliation query before/after shows the gap closed.
- [ ] After Phase 0/Layer 2 fix: trigger one local evolution run and confirm its `cost_usd>0` invocations now have joining `llmCallTracking` rows (per-run SUM matches).

## Verification

### A) Playwright Verification (required for UI changes)
- [ ] Load `/admin/costs` on a local server; confirm past-7-day total matches the reconciliation truth (only if dashboard read-path changes).

### B) Automated Tests
- [ ] `npm run test:unit -- costAnalytics`
- [ ] `npm run test:unit -- llms` (Layer 1 evolution-throws + main-app-swallows + kill-switch precedence)
- [ ] `npm run check:llm-coverage` (Layer 2 guard green)
- [ ] `npm run test:integration -- --grep "cost attribution"` (canonical exact-parity + Layer 3 reconciliation; the existing describe is `'Per-purpose cost attribution integration tests'` — keep new cases under a title this grep matches, or update the command to the chosen title so they stay consistent)
- [ ] `npm run migration:verify` — REQUIRED (the `idx_invocations_created_at` migration touches `supabase/migrations/**`; ephemeral Docker postgres is the real idempotency gate, since `lint:migrations` has no index-idempotency coverage beyond the `IF NOT EXISTS` pattern)

## Documentation Updates
The following docs were identified as relevant and may need updates:
- [ ] `evolution/docs/cost_optimization.md` — update the audit-gap caveat with the final past-week resolution + any backfill performed.
- [ ] `docs/feature_deep_dives/admin_panel.md` — note any dashboard read-path / fallback change.
- [ ] `evolution/docs/metrics.md` — if the cost read path changes.
- [ ] `evolution/docs/evolution_metrics.md` — currently a stub; fill in if cost rollups are touched.
- [ ] `evolution/docs/data_model.md` — if any cost schema/RPC changes.
- [ ] `evolution/docs/reference.md` — if `costAnalytics.ts` behavior changes.
- [ ] `docs/feature_deep_dives/metrics_analytics.md` — likely no change (user-engagement metrics).

## Rollback & Risk
- **Canonical merge (Phase 1):** gated by env flag `COST_DASHBOARD_UNIFIED_EVOLUTION` — unset reverts `/admin/costs` to the current `llmCallTracking`-only reads with zero code rollback. No reader is repointed at a removed source; the old query paths stay intact behind the flag.
- **Migration (the one `idx_invocations_created_at` index):** `CREATE INDEX IF NOT EXISTS` (idempotent, re-appliable, drop-only rollback). Gate with `npm run migration:verify` (ephemeral Docker postgres). No view/RPC is added or repointed, so `get_llm_spend_buckets`/`daily_llm_costs` are untouched and need no staged `_v2` swap.
- **Layer 1 (Phase 3):** evolution stays fail-closed (already the case); kill-switch `LLM_REQUIRE_TRACKING_DISABLED` reverts without deploy. Main-app default is NOT changed (out of scope), so no main-app availability risk.
- **Layer 3 monitor:** staging-only, alert-only (files an issue) — no production write/availability impact. Expected RED until the write path is fixed; do not gate merges on it until Phase 0/Layer 2 lands.
- **Prod schema skew:** prod `llmCallTracking` lacks the `is_test` column (migration unpromoted). This project is dev-only; the merge/`is_test` logic must NOT ship to the prod (evolution) host until that migration is promoted, or prod queries error. Tracked as a pre-prod-rollout gate, not a dev-scope task.

## Review & Discussion

### Iteration 1 (2026-06-23) — all reviewers 2/5 → gaps fixed
- **Security/Technical (2/5):** Layer 1 pointed at a log line (`llms.ts:286`) not the gate (`:301`) and described a nonexistent `?? false` default; system-wide fail-closed flip = unscoped blast radius; dedup predicate unspecified; `is_test` is a 3-table join. → **Fixed:** Layer 1 rescoped (evolution-only + kill-switch, no global flip, correct line 301); dedup = `call_source NOT LIKE 'evolution_%'` filter (no FK join); `is_test` 3-table join + NULL⇒real spelled out.
- **Architecture/Integration (2/5):** wrong file path (`src/lib/services/costAnalytics.ts` doesn't exist); DB-UNION unsound (no `model`/`tokens`/`userid`/`call_source`/`is_test` columns; per-phase vs per-call grain); should reuse `getRunCostsWithFallback`/`evolution_metrics`; missing `created_at` index. → **Fixed:** switched to **app-level merge** sourcing evolution from run-grain `getRunCostsWithFallback`; rejected DB-UNION (Option B-DB); corrected all paths to `evolution/src/services/costAnalytics.ts`; added `created_at` index note.
- **Testing/CI (2/5):** wrong test paths; E2E spec name collision (`admin-evolution-cost-split.spec.ts` exists); `lint:migrations` doesn't cover VIEW idempotency; Layer 1 no blast-radius test/kill-switch; Layer 3 wrong precedent (`evolution-run-health.yml` is Slack-only) + claim-gate. → **Fixed:** corrected paths to existing files; renamed E2E to `admin-costs-evolution-total.spec.ts`; `migration:verify` named as the real gate; added main-app swallow regression test + `LLM_REQUIRE_TRACKING_DISABLED` kill-switch; Layer 3 re-specified on `evolution-cost-alarm.yml` (issue-filing, staging-only) with per-run SUM + `allow_test_execution:true` seeding.

### Iteration 2 (2026-06-23) — all reviewers 4/5 → remaining gap fixed
- **Security/Technical (4/5):** all iter-1 blockers verified resolved; ONE remaining blocking issue — **time-anchor/grain mismatch**: evolution helper bucketed by `run.created_at` (run-grain `evolution_metrics` rollup) while the reconciliation oracle/verification use `invocation.created_at` + live invocation SUM → can't match at window edges; in-flight runs under-report (rollup written only at finalization). Architecture (4/5) and Testing (4/5) had **0 critical gaps** (only minors: `getDailyCostsAction` view exception, parity-wording, schedule-only Layer 3, kill-switch precedence test, time-bucket boundary test, conditional `migration:verify`).
- **Fix:** switched evolution sourcing to **invocation grain** — `SUM(evolution_agent_invocations.cost_usd)` by `invocation.created_at`, extending the existing `getEvolutionReconciliationAction` (single shared helper → exact parity, counts in-flight spend). Added an unconditional `idx_invocations_created_at` migration + `migration:verify` as a required check. Mandated `attributeCallSource(...).category !== 'evolution'` as the dedup predicate. Excluded `getDailyCostsAction` (view has no `is_test`/`call_source`) with a documented caveat. Made Layer 3 explicitly schedule-only / not a required check. Added kill-switch-precedence + time-bucket-boundary unit tests; parity assertion now "exact," not "within tolerance."

### Iteration 3 (2026-06-23) — ✅ CONSENSUS: Security 5/5, Architecture 5/5, Testing 5/5
The iteration-2 anchor blocker is resolved and code-verified by all three reviewers: `getEvolutionReconciliationAction` (`evolution/src/services/costAnalytics.ts:692`) already SUMs `evolution_agent_invocations.cost_usd` by `invocation.created_at`, so sourcing the dashboard from that same (refactored shared) helper gives exact parity by construction; the dedup predicate is code-equivalent to the oracle's existing filter; `idx_invocations_created_at` is genuinely absent and the migration is idempotent + `migration:verify`-gated. No critical gaps remain. Cheap clarity nits folded in this iteration: shared-helper `includeTest`/window parity pinned, `unattributed:`→non-evolution test assertion added, `--grep` title consistency fixed.

**Plan was READY FOR EXECUTION** (then extended with direct-provider tracking — re-reviewed below).

### Iteration 4 (2026-06-23) — Architecture 5/5, Testing 5/5, Security 4/5 → gaps fixed
Re-review of the NEW direct-provider-tracking work (extract shared tracked provider, route `run-evolution-local.ts` through it, remove allowlist entry).
- **Architecture 5/5** (0 critical): factory location (`evolution/src/lib/pipeline/infra/`) + mock-vs-tracked separation + `callLLM` CLI-safety + `{text,usage}`-vs-string non-issue all code-verified. Flagged a factual correction (folded in): the script ALREADY direct-inserts `llmCallTracking` (lines 227/294) but unjoinably (`call_source='evolution_v2_*'`, all-zero userid, NULL FK, swallowed) — Phase 0 premise corrected from "zero rows."
- **Testing 5/5** (0 critical): all new test items verify; flagged that removing the allowlist entry requires deleting the bypass code, not just swapping call sites.
- **Security 4/5** (2 critical, FIXED): (1) **deleting the allowlist entry without deleting `createDirectLLMProvider`'s SDK calls + the two `llmCallTracking.insert`s + orphaned imports turns `check:llm-coverage` RED** → Layer 2 now mandates full function+import deletion and a real-file guard run. (2) **`LLMSpendingGate` fail-closes using its own `createSupabaseServiceClient` (ignores `trackingDb`) and blocks on DB-error / missing env** → Layer 2 now requires real DB env for real-LLM runs + a required `--ungated` bypass flag, with `--mock` as the DB-less path. Also folded the factory-signature args and the `--model` registry-constraint trade-off.

### Iteration 5 (2026-06-23) — ✅ CONSENSUS: Security 5/5, Architecture 5/5, Testing 5/5
Both iteration-4 blockers verified resolved against real code: (1) the coverage guard scans raw file text (`check-llm-call-coverage.ts:77`) and matches all three bypass patterns present in `createDirectLLMProvider` (210/276/227/294), so the mandated full-function+imports deletion + real-file `check:llm-coverage` run is correct and necessary; (2) `callLLM:988` calls `getSpendingGate().checkBudget()` unconditionally using its own service client (ignores `trackingDb`) and fail-closes on DB/auth error (`llmSpendingGate.test.ts:136-137`), so the required-DB-env + required `--ungated` (high-caps dev DB) deliverable is justified. Phase 0 corrected premise (unjoinable `evolution_v2_*` rows) and the factory signature match the source byte-for-byte. Final executor nits folded in: `--ungated` mechanism specified (high dev-DB caps, no per-call skip), keep `SupabaseClient` import + `evolution_runs.insert`, both `createDirectLLMProvider` call sites (434/443) covered by full deletion.

**Plan is READY FOR EXECUTION (final).**
