# Playwright Sweep — Evolution Admin UI Findings

Sweep target: **100** distinct bugs or UX issues across the evolution admin UI.
Environment: local dev server `http://localhost:3500`, admin `abecha@gmail.com`, headless Playwright MCP (Chromium).
Started: 2026-06-23.

Severity legend: **S1** crash/data-loss/broken core flow · **S2** functional bug (feature wrong/broken) · **S3** UX problem (confusing, missing feedback, layout) · **S4** polish/nit · **A11y** accessibility.

| # | Route | Category | Sev | Issue | Repro / Expected vs Actual |
|---|-------|----------|-----|-------|----------------------------|
| 1 | /admin/evolution-dashboard | Bug | S2 | "Hide test content" leaks runs whose strategy name uses the `[TESTEVO]` prefix (no underscore) | With "Hide test content" CHECKED, Recent Runs still shows `[TESTEVO]-FR3-canary-B2-4d-fixed-…`. The `is_test_content` classifier matches `[TEST]`/`[TEST_EVO]`/`[E2E]` but not `[TESTEVO]`. Expected: any bracketed TEST* prefix hidden. |
| 2 | /admin/evolution-dashboard | Bug | S3 | **FIXED (gate runs); RETRACTED (smoke fixture)** | "Gate verify/final real …" runs now caught by the broadened classifier (#9 fix). "Nightly smoke fixture" is **intentionally** visible per `20260621000002_evolution_nightly_smoke_fixture.sql` (operational fixture, deliberately not flagged) — not a bug. |
| 3 | /admin/evolution-dashboard | UX | S3 | Dashboard auto-refresh briefly tears down on filter toggle; transient "Failed to fetch" server-action errors surface in console with no user-facing toast/recovery state | Toggling "Hide test content" during the 15s auto-refresh window can race the in-flight server action; user sees stale data with no indication. (Note: also coincided with a dev-server drop — revisit severity once reproduced on a stable build.) |

| 4 | /admin/evolution/runs | UX | S4 | Page heading missing space before count: renders "Evolution Runs(257)" | `main h1` text = `Evolution Runs(257)`. Expected "Evolution Runs (257)". Count is concatenated directly to the title. |
| 5 | /admin/evolution/runs | Bug | S2 | Test-content leak (#1/#2) also affects the main Runs list, not just the dashboard | With "Hide test content" CHECKED, `/runs` (257) shows many `[TESTEVO]-FR3-canary-…`, "Gate verify/final real …", "Nightly smoke fixture" rows. Confirms the leak is in the shared `is_test_content` classifier, surfacing on every list page. |

| 6 | /admin/evolution/runs | Bug/UX | S2 | Runs table is not sortable on any column | All 19 `<th>` lack a sort button, `aria-sort`, and pointer cursor. Users cannot sort 257 runs by Spent, Max Elo, Variants, Created, etc. `visualization.md` documents column-level sort via `onSort`; not wired here. Also an a11y gap (no `aria-sort`). |

| 7 | /admin/evolution/experiments | UX/Perf | S3 | Experiments list renders all 107 rows with no pagination | No Prev/Next/Last or jump-to-page control; `tbody` has 107 `<tr>`. Inconsistent with Runs (paginates ~20/page) and a perf concern as experiment count grows. |
| 8 | /admin/evolution/experiments | A11y | S4 | Actions column has an empty `<th>` header | Last `<th>` text is empty; screen readers announce a blank column header. Add visually-hidden "Actions" label. (Shared `EntityListPage`/`RegistryPage` pattern — recurs on other list tables.) |
| — | (systemic) | — | — | **#4 generalizes**: `EntityListPage` heading concatenates count with no space on every list page (`Experiments(107)`, `Evolution Runs(257)`, …). **#6 generalizes**: no list table (`Runs`, `Experiments`) exposes sortable columns. Counted once each above. |

| 9 | /admin/evolution/strategies | Bug | S2 | **FIXED** — root of #1/#5: test-content classifier gap | `is_test_content` classifier missed `[TESTEVO]-…` (bracket, no underscore) and timestamps that were trailing or space/underscore-delimited (`Gate verify real 1782140234529_7fe0bd`). **Fix shipped:** broadened `isTestContentName` (`evolution/src/services/shared.ts`, added `[testevo]` + regex `(^\|[-_ ])\d{10,13}([-_ ]\|$)`) + matching `evolution_is_test_name` Postgres fn via migration `20260623000001_evolution_is_test_name_broaden_patterns.sql` (re-flags strategies/prompts/experiments). Verified: 44 unit tests + isolated-Postgres regex check (`t\|t\|t\|t\|f\|f\|f`). Anti-drift `TEST_NAME_FIXTURES` updated. (Will fully take effect once the migration deploys to staging via CI.) |

| 10 | /admin/evolution/strategies/new | UX | S4 | Inconsistent wizard defaults: Generation Model requires explicit choice but Judge Model is pre-filled | Strategy wizard: Generation Model defaults to "Select a model…" (required) while Judge Model pre-selects `DEFAULT_JUDGE_MODEL` = `qwen-2.5-7b-instruct`. **CORRECTION after code check:** qwen-2.5-7b-instruct is `provider:'openrouter'` (cheap, deployable — NOT local), and it's the intended canonical default judge used consistently across all 5 judge surfaces (wizard, Judge Lab, agreement, weight-inference, match re-judge). So this is **not** a functional bug — downgraded to a minor UX inconsistency (one of the two model pickers defaults, the other doesn't). Original S2 "non-functional off-Vercel" claim retracted. |

| 11 | tactics, judge-rubrics, prompt-editor, weight-inference, variants/[id] | UX/SEO | S3 | Generic "ExplainAnything" browser title on ≥5 routes | `document.title` = "ExplainAnything" on Tactics, Judge Rubrics, Prompt Editor, Weight Inference, **and** the Variant detail page (while Runs/Strategies/Run-detail/etc. correctly set "… \| Evolution"). Widespread missing route `metadata`/`<title>` — affects Tools pages AND at least one detail page → ambiguous browser tabs/history/bookmarks. |
| 17 | /admin/evolution/judge-rubrics | UX | S4 | Inconsistent "New …" button label casing + heading-count handling across list pages | "New rubric" (sentence case) vs "New Fingerprint" / "New Strategy" (Title Case). Also Judge Rubrics heading has no `(count)` while Tactics/Runs/etc. do. Cosmetic inconsistency in the shared list-page chrome. |
| 12 | /admin/evolution/runs,experiments,strategies | UX | S3 | Inconsistent sortability: leaderboards (Tactics/Criteria) have sortable columns (▲▼) but the primary entity lists (Runs/Experiments/Strategies) have none | Same design system, divergent capability. Users reasonably expect the high-volume Runs/Experiments tables to sort like the leaderboards do. (Companion to #6.) |

| 13 | /admin/evolution/variants | UX | S3 | **FIXED (merged into #14)** | Re-analysis: the ▲ was EntityTable's *inactive* sortable indicator (muted, "click to sort"), not an active-ascending-sort arrow — so the only real issue is that the sort was dead (#14). Corrected characterization; fixed by the #14 change. |
| 14 | /admin/evolution/variants | Bug | S2 | **FIXED** — Rating column advertised sortability but clicking did nothing | Confirmed real on a stable server (order unchanged after click). Root cause: `COLUMNS` marked Rating `sortable: true` (→ EntityTable renders ▲ + `cursor-pointer`), but the page never passed `onSort` and `listVariantsAction` orders by `created_at desc` with no sort param — so EntityTable's `onClick={sortable && onSort ? … : undefined}` was undefined (dead click). **Fix shipped:** removed the false `sortable` flag (`src/app/admin/evolution/variants/page.tsx`) until server-side sort is wired. Verified live: header now plain "Rating", no ▲, `cursor:auto`, not clickable. 13 page tests pass. |

| 15 | (all evolution dialogs) | A11y | S3 | Radix dialogs missing `aria-describedby` — systemic | Console warns "Missing `Description` or `aria-describedby={undefined}` for {DialogContent}" on the New Style Fingerprint dialog AND the New Prompt dialog (2 warnings each). Affects the shared `FormDialog`/`ConfirmDialog`. Screen readers can't announce dialog purpose. |
| 16 | /admin/evolution/style-fingerprints | UX/A11y | S3 | Invalid-name save shows a generic "Data validation failed" with no field-level detail | Saving name "bad name [test]" (spaces/brackets — placeholder says "no spaces") surfaces only a red "Data validation failed". No message says *which* field or *why*; the Name input gets no `aria-invalid`. Expected: field-level error like "Name: letters/digits/_/- only, no spaces". |

| 18 | /admin/evolution/runs/[id]?tab=metrics | UX | S3 | Raw metric identifiers leak into the Metrics tab as uppercased keys | Labels like "GENERATE_FROM_PREVIOUS_ARTICLE / GROUNDING_ENHANCE / 0 / 10 (BUCKET)" and "PARAGRAPH_RECOMBINE_WITH_COHERENCE_PASS / … / -10 / 0 (BUCKET)" render the raw dynamic metric keys (eloAttrDeltaHist/bucket families) instead of human-readable names. Hard to parse for researchers. |
| 19 | /admin/evolution/runs/[id]?tab=metrics | UX | S4 | Metric value abuts its confidence interval with no space | Renders "1151[951, 1352]", "1247[1053, 1441]" — value and `[lo, hi]` CI concatenated. Expected "1151 [951, 1352]". (Same missing-space family as #4.) |

| 20 | /admin/evolution/prompt-editor, /weight-inference | UX | S4 | Breadcrumb has no linked "Evolution" parent | Breadcrumb renders static text ("Prompt Editor" / "Implied Rubric Weights") with zero links on both pages. Every other evolution page's breadcrumb starts with a clickable "Evolution" → dashboard. Inconsistent nav; no quick way back. (Same newer-Tools-routes cluster as #11.) |
| 23 | /admin/evolution/weight-inference | UX | S3 | "Create session" is a silent no-op on an empty form — enabled button, no feedback | Clicking "Create session" with no Name, no topic/test-set, and 0 criteria selected does nothing: no session created, no error, no toast, no hint, button not disabled (`disabled=false`, no `title`/`aria-disabled`). User can't tell what's required or why nothing happened. Expected: disable until valid (with hint) or show field errors. |

| 21 | /admin/evolution/{prompts,style-fingerprints} | UX/A11y | S3 | Inconsistent FormDialog validation; no `aria-invalid` on invalid fields | New Prompt dialog (empty Save) relies on native HTML5 `required` (native bubble on first field); New Style Fingerprint dialog surfaces an app-level generic "Data validation failed" (#16). Two different validation UXs from the same `FormDialog` family. Neither sets `aria-invalid` on the offending input → screen readers don't flag the bad field. |

| 22 | /admin/evolution/start-experiment | UX | S4 | Paragraph-prompt radio labels are redundant + missing a space before the em-dash | Prompt picker renders "[para] 10.P11— [para] 10.P11" — the prompt name is duplicated as its own preview/description, and there's no space before "—" (reads "P11— [para]"). Affects all `[para]` prompts. Expected: show the name once, or a real preview, with "name — preview". |

| 24 | /admin/evolution/runs/[id]?tab=cost-estimates | UX | S3 | Summary "ERROR %" headline is inconsistent with its own "ABS ERROR" / totals | Summary block: TOTAL $0.07, ESTIMATED $0.07, ABS ERROR $0.001 (→ ~1.4% overall), but headline **ERROR % = −24.9%**. The −24.9% matches only the `generate_from_previous_article` agent row; the run's two estimable agents (+27% and −24.9%) nearly cancel at the aggregate. Showing a single agent's per-invocation error as the unqualified headline "ERROR %" next to a ~1.4% ABS ERROR is misleading. Clarify the label (e.g. "Avg per-invocation error") or compute the run-level figure. |

<!-- findings appended below -->

## Running notes
- Sidebar exposes routes NOT in `evolution/docs/visualization.md`: **Style Fingerprints** (`/admin/evolution/style-fingerprints`), **Judge Rubrics** (`/admin/evolution/judge-rubrics`). Doc drift — add to Phase-5 doc-update list.
- DB has real data: 235 completed runs, 16 failed, 254 total, $9.64 total cost.
